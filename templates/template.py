"""Reusable DSA problem template.

Problem:
    Write the problem statement in your own words.

Pattern:
    Name the core pattern, such as two pointers, sliding window,
    binary search, BFS, DFS, dynamic programming, greedy, stack,
    heap, prefix sum, or union find.

Approach:
    1. State the brute-force idea.
    2. Identify the bottleneck.
    3. Explain the optimized approach.
    4. Write time and space complexity.
"""


class Solution:
    """Place the final implementation here after working through hints."""

    def solve(self):
        """Replace this method with the function required by the problem."""
        raise NotImplementedError("Implement the solution for this problem.")


if __name__ == "__main__":
    solution = Solution()
    # Add quick manual checks here, for example:
    # assert solution.solve(...) == ...
